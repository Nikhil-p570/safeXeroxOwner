name: safe_xerox_owner
description: Xerox Shop Owner app for Safe Xerox.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  google_fonts: ^6.0.0
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0
  qr_flutter: ^4.1.0
  printing: ^5.13.0
  pdf: ^3.10.8
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
